# macOS
.DS_Store

# Node
node_modules/

# Secrets — never commit API keys
.env
*.env
.env.local

# Claude Code local settings (may contain machine-specific allow-list / keys)
.claude/settings.local.json

.playwright-mcp/
~$*

# 별개 저장소(0626_new_works)라 추적 제외
0609NewTesttt/Week5/Week5-1/
