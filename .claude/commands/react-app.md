---
description: CDN 기반 React + Tailwind로 단일 index.html 웹앱 생성
argument-hint: [만들고 싶은 앱 설명]
---

`single-react-dev` 에이전트를 사용해 아래 요구사항을 단일 `index.html` 파일로 구현해줘.

요구사항: $ARGUMENTS

- 반드시 `single-react-dev` 서브에이전트(Task 도구)로 작업을 위임할 것
- CDN 기반 React 18 + ReactDOM + Babel + Tailwind CSS 사용
- 파일은 `index.html` 하나만 생성
- 기본 UI 언어는 한국어
- 완성 후 실행 방법(로컬 서버)도 안내
