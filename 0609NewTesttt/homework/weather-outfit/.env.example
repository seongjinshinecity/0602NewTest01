# OpenWeatherMap API 키 (https://openweathermap.org/api 에서 발급)
# 콤마(,)로 여러 개 지정하면 교대(round-robin)로 사용하고, 401/429 시 다음 키로 폴오버합니다.
# 비워두면 서버가 자동으로 목(mock) 데이터로 동작합니다.
OWM_API_KEYS=key1,key2

# 좌표/도시 입력이 없을 때 사용할 기본 위치
DEFAULT_CITY=Seoul
PORT=3000
