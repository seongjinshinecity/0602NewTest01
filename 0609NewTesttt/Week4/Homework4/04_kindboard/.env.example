# ── 데이터베이스 (선택) ──────────────────────────────────────
# Supabase 대시보드 → Project Settings → Database → Connection string(URI) 복사.
# 없으면 자동으로 임베디드 PostgreSQL(PGlite, ./data)을 사용합니다.
#
# DATABASE_URL=postgresql://postgres:YOUR-PASSWORD@db.xxxxxxxxxxxx.supabase.co:5432/postgres

# ── 포트 (선택) ─────────────────────────────────────────────
# PORT=3005
